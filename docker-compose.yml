version: '2.1'

services:
  base:
    container_name: base
    user: root
    env_file:
      - .env
    image: base
    build:
      context: base
      args:
        - TZ=America/Los_Angeles
    entrypoint: /var/lib/base/entrypoint.sh
    healthcheck:
      test: test -e /fuel-ui/npm_install.log
    volumes:
      - ./base:/var/lib/base
      - ./fuel-ui:/var/lib/fuel-ui
      - ./fuel-web:/var/lib/fuel-web

  test:
    container_name: test
    image: base
    user: root
    env_file:
      - .env
    volumes:
      - ./test:/var/lib/test
      - ./fuel-ui:/var/lib/fuel-ui
      - ./fuel-web:/var/lib/fuel-web
    entrypoint: /var/lib/test/entrypoint.sh
    ports:
      - "${NODE_PORT}"
      - "9000"
    depends_on:
      base:
        condition: service_started
      dev-server:
        condition: service_healthy
      selenium:
        condition: service_started

  db:
    container_name: db
    image: postgres:9.5
    ports:
      - "${NAILGUN_DB_PORT}"
    environment:
      - POSTGRES_PASSWORD=${DB_ROOTPW}
      - POSTGRES_USER=${DB_ROOT}
      - POSTGRES_DATABASE=${NAILGUN_DB}

  dev-server:
    container_name: dev-server
    user: root
    env_file:
      - .env
    build:
      context: dev-server
      args:
        - FUEL_WEB_REQS=${FUEL_WEB_REQS}
    volumes:
      - ./dev-server:/var/lib/dev-server
      - ./fuel-ui:/var/lib/fuel-ui
      - ./fuel-web:/var/lib/fuel-web
    entrypoint: /var/lib/dev-server/entrypoint.sh
    ports:
      - "${NAILGUN_PORT}"
      - "${DEV_SERVER_PORT}"
    healthcheck:
      test: curl -s -w %{http_code} -o /dev/null http://dev-server:${NAILGUN_PORT}/api/version | grep -q 401
      interval: 20s
      timeout:  10s
      retries: 10

    depends_on:
      db:
        condition: service_started

  selenium:
    container_name: selenium
    build:
      context: selenium/
      args:
        - GECKODRIVER_VERSION=0.15.0
        - FIREFOX_VERSION=46.0
    env_file:
      - .env
    volumes:
      - ./selenium:/var/lib/selenium
    user: seluser
    entrypoint: /var/lib/selenium/entrypoint.sh
    ports:
      - "${SELENIUM_SERVER_PORT}"
